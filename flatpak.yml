name: My App
description: My application
version: 1.0

builds:
  - src: .
    url: github://MyGitHubUsername/MyGitHubRepository.git
    branch: main
    build-script: |
      # Build your application here
      echo "Building my app"
      make build
      
github-actions:
  workflow: github-actions.yaml
